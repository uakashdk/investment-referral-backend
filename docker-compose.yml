version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: investment_backend

    ports:
      - "${PORT}:${PORT}"

    env_file:
      - .env

    volumes:
      - .:/app
      - /app/node_modules

    restart: unless-stopped

    command: npm run dev